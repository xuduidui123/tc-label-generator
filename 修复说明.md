# 修复说明（相对原 app.py）

本目录是原项目的**修复版副本**，原始文件未做任何改动。

## 一、你报告的 Bug：999 订单 CE 后 5 位前导 0 加粗丢失
- 现象：后 5 位如 `01984` 只加粗了 `1984`，前面的 `0` 丢失/未加粗。
- 根因：原代码 `pd.read_excel` 只对少数列指定了 `dtype=str`，`CON加粗`、`SKU`、`CEORMSNO` 等号码列未指定，被 pandas 当成数字读入，`01984` → `1984`，前导 0 在读取阶段就没了。
- 修复：数据源改为 **全列按字符串读取** `pd.read_excel(uploaded_file, dtype=str)`。后 5 位加粗的切分改用统一函数 `split_orms_bold()`，纯字符串切片，保留前导/内部 0。
- 前提：Excel 中该号码若被存成“数字”，仍需把单元格格式设为“文本”，否则 0 在文件里本就不存在（详见 README）。

## 二、审查发现的高危缺陷（一并修复）
1. **ITF 奇数位静默补 0**（致命）：python-barcode 遇奇数位会悄悄补 0，导致条码与目视号码不符。新增 `validate_itf()`：奇数位/非数字直接报错。
2. **条码值与目视号码不一致**（致命）：条码来自 `ITF`/`BD条码号`，目视来自 `…加空格` 列，二者可能被改花。新增校验：目视号码去空格后必须等于条码值，否则报错。
3. **同名文件静默覆盖 + 成功数虚高**：多行命名相同会互相覆盖，但计数照加。新增 `used_names` 去重拦截；最终以磁盘实际 PDF 数为准报告数量。
4. **`row.get(col, default)` 兜底失效**：列存在但单元格为空(NaN)时返回的是 NaN 而非 default，导致 ITF 分支里 ORMS 派生的 CON 值被忽略。改为显式判空后再回退。
5. **ORMS 取列两处不一致**：外箱用模糊匹配、ITF 用硬编码 `CEORMSNO`。统一为 `get_orms()`。
6. **缺列静默出空白**：新增 `validate_columns()`，各任务必填列缺失时直接报错并中止。
7. **LibreOffice 返回 0 却没出 PDF 时无声消失**：`_confirm_pdf()` 找不到 PDF 就报错，进入失败列表。
8. **XML 转义只处理了 `&`**：改用 `xml.sax.saxutils.escape` 处理 `& < > " '`，避免品名含特殊字符时破坏 docx。
9. **ZIP 路径固定共享**：改为 `Tesco_Labels_<uuid>.zip`，避免多用户/多标签页互相覆盖。
10. **文件名路径穿越**：`sanitize_name()` 处理 `/ \ ..` 并取 basename，防止越界写入。
11. **LibreOffice 并发锁**：转换时用独立 `-env:UserInstallation` 配置目录，降低多会话“another instance”冲突。

## 四、合并数据源版改造（第二轮）
- **两源合一**：`1. 外箱内盒数据源` 与 `1.1 标贴数据源` 合并为一张主表，四类标签只填一次。空白模板见 `Tesco数据源模板.xlsx`（网站也可下载）。
- **每行开关列**：`是否要做外箱/内盒`、`是否需要ITF-UK/ITF-CE`、`是否要做BD`（Y/N），逐行控制生成。网站顶部再勾选本次要跑哪些类型。
- **自动命名**（删掉 5 个手填命名列）：
  - 外箱 `{PO}{品名}{总外箱数}个外箱`；内盒 `内盒-{品名}`；
  - ITF-UK `ITF-UK-{品名}-{英文名}`；ITF-CE `ITF-CE-{品名}-{英文名}`；BD `BD-{品名}`。
- **自动推导**（删掉对应手填列）：`CON`=不加粗+加粗、`外箱尺寸`=长×宽×高、UK/CE 由 PO 前缀判断、`是否有CDU` 由加印文字是否为空判断。
- **仍手填**：`ITF加空格`/`BD条码号加空格`（分组空格无规律），程序只做一致性校验。
- **网站新增**：生成前「体检」（缺列/ITF奇数位/条码不符/命名重复）、逐行结果明细表、首张 PDF 预览、空白模板下载、按类型分子文件夹打包。
- 预览依赖 `poppler-utils`（已加入 `packages.txt`）；缺失时自动跳过预览、不影响生成。

## 五、按真实公式重建数据源表（第三轮）
用户解开原表保护后，按其真实的三色结构重做：
- **绿色手填**（A–S 顺序固定）+ **橙色公式列**（Excel 自动算）+ **黄色人工选择**（下拉）。
- **修好原表坏公式**：`外箱BD号`（原 `#REF!` → `IFERROR(IF(外箱装量/内盒装量=1," ","B/D "&比值),"")`）、`ITF_UK命名`/`ITF_CE命名`（原 `#REF!` → 用户新规则 `ITF-UK/CE-品名-英文名`）。
- **还原保留的公式**：双人抬（毛重阈值 380>23 / 999>14）、BL（英文名判断）、ITF加空格（3-5-6 分组）、BD条码号（`02`+ITF+`37`+装量比）、BD条码号加空格、CON不加粗/加粗（LEFT/RIGHT CEORMSNO）、外箱尺寸、订单国家、胶带颜色（XLOOKUP 选项字典）、CDU 文案、外箱/内盒命名。
- **程序改为读取公式列**：`resolved_name()` 优先读命名列（Excel 算好的值），为空才回退内置规则；双人抬/BL/尺寸/条码等一律读公式输出，不再在 Python 里另算，避免与 Excel 逻辑分叉。
- **验证**：用 LibreOffice 真实重算模板，13 项公式（含双人抬阈值、`01984` 前导0、BD条码号、ITF-CE命名、外箱BD号）全部正确；再把重算后的模板喂给程序端到端，5 个 PDF 命名全部来自公式列、零失败。
- 注意：数据源必须先用 Excel 打开保存让公式计算，再上传。

## 六、本地测试反馈修复（第四轮）
1. **外箱/内盒无 Brand Logo**：根因是数据里英文名词间为不间断空格（`Go⍽Cook`），原 BL 公式用普通空格 `SEARCH` 匹配不上，返回空。修复：① Excel 公式改用 `SUBSTITUTE(…,UNICHAR(160)," ")` 归一化后再判断；② 程序加 `resolve_bl()`，BL 列为空时按归一化英文名兜底判 Tesco/FF。
2. **内盒品名换行**：品名在文本框内（docxtpl RichText 在文本框里不生效），改为在 `模板_InnerCarton.docx` 里把品名白字 run 由 5pt 调小到 4pt，长名单行不换行、白字可见。
3. **外箱 CE ORMS item number 标签换行**：`模板_OuterCarton.docx` 中该标签两个格子字号由 6.5pt 调小到 5.5pt，单行显示。
4. **行号一致**：预览表加「Excel行号」列（第2行起，与报错“第N行”一致，消除 0 基索引困惑）。
5. **体检提示**：数据源未在 Excel 保存（公式列全空）时给出明确提示。
以上均以 LibreOffice 实际渲染 PDF 截图确认。

## 七、第二轮本地测试反馈修复（第五轮）
按客户业务规则与实测反馈：
1. **ITF/BD/内盒 一款产品只做一次**：同款产品多个 PO 行不再报“命名重复”，自动去重只生成一次（其余记为“跳过”，非失败）。外箱仍按每个 PO 生成。
2. **无内盒不出 BD**：内盒装量=外箱装量 视为无内盒 → 不做内盒、不做 BD（BD条码号 公式此时为空），生成时自动跳过，不再报“BD 条码值为空”。规则：有内盒→内盒贴 ITF、外箱贴 BD；无内盒→外箱直接贴 ITF、无 BD。
3. **体检不再误报**：去重、无内盒无 BD 属正常，不再列为隐患；只报真正的数据错误（PO 前缀、ITF 位数、条码不一致）。
4. **字体统一**：原来外箱/内盒用微软雅黑、ITF 用宋体/Arial、BD 默认 Times，四模板三种字体（微软雅黑在 Mac 缺失时回退成衬线）。已把 4 个模板全部统一为微软雅黑；并让程序在 macOS 上把字体装到 `~/Library/Fonts`，确保本地与云端渲染一致。
5. **内盒 Brand Logo 被遮挡**：Logo 高度 3mm 超出较窄的内盒黑条、顶部溢出被交界遮住，改为 2.2mm 后完整落在黑条内。
以上均以 LibreOffice 实际渲染 PDF 截图逐项确认。

## 八、第三轮本地测试反馈修复（第六轮）
1. **外箱黑条粗细不一**：模板里纯黑条行有的是 `atLeast 3mm`、有的是自动高度（会随内容长高），导致同一文件内左右两半黑条一厚一薄。已把所有"纯 Logo+品名黑条行"统一为固定行高 3.5mm（`exact`），四处黑条厚度一致，Logo 完整不裁。
2. **ITF/BD 分页（要求四种标都单页）**：字体统一为微软雅黑后行高变大，加上模板尾部空段落，使 ITF、BD 的号码行溢出到第二页。已：① 清理 ITF/BD 模板的引导/间隔空段落并压上下页边距；② ITF 条码行的固定最小行高 30.9mm→22mm、条码图 28→20mm；③ BD 条码图 70→60mm。现四种标签（外箱/内盒/ITF/BD）均为**单页**。
以 pdfinfo 逐一核验页数=1，并渲染确认版式完整。

## 九、第四轮本地测试反馈修复（第七轮）
1. **ITF 必须对齐客户标准格式**（附件参考 PDF）：上一轮把 ITF 字体统一成微软雅黑，反而改变了 ITF 版式并撑成两页。已**还原原始 `模板_ITF.docx`**（Arial 粗体、细边框、高条码，与参考一致），条码高度还原 28mm；BD 同样还原原始 `模板_BD.docx`+条码 70mm。二者渲染均为单页且与原始一致。
2. **ITF 品名绝不换行**：品名自适应字号原来用 Verdana 测量，但实际渲染字体更宽导致长名仍换行。改为**品名强制微软雅黑并用同款字体测量**（测量=渲染，宽度必匹配）、目标宽度收窄到 64mm、最小 3pt——短名正常、超长名自动缩小到单行不换行。
3. **内盒黑条仍厚**：与外箱同因（自动高度行随内容长高）。已把内盒所有"纯 Logo+品名黑条行"统一为固定行高 3.5mm，四处黑条一致变薄。
4. 模板 xlsx 统一为单一正确文件 `Tesco数据源模板.xlsx`（46 列三色版），删除临时的 _v2。
说明：ITF/BD 用原始模板（各自客户标准格式），外箱/内盒用微软雅黑（并在 Mac 安装字体避免衬线回退）——各标签均对齐其应有格式。

## 十、内盒黑条多余黑线（第八轮）
内盒黑条由 tbl0 的两行交错拼成（2-up：左份的条在下面一行、右份在上面一行）。上一轮把这两行强制成固定 3.5mm(exact) 小于自然内容高度，导致交错的黑格错位、在左侧两份黑条下方露出一条细黑线。改为让这些行用 `atLeast 4.1mm`（内容不超高就保持该高度、且避免 exact 的错位），四份黑条干净一致、无多余线，仍单页。

## 三、未改动 / 需注意
- 字体自适应的经验系数（`0.80 / 288DPI`）沿用原值；若 LibreOffice 版本变化导致品名换行，需要重新标定。
- 条码未强制校验 ITF-14 校验位（避免误伤内部编码），仅校验位数与数字。
- 版式模板（4 个 docx）、Logo、字体均直接复用原文件，未改动。
